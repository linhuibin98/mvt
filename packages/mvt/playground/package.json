{
  "scripts": {
    "dev": "npm run debug && mvt --jsx-factory=h --open",
    "build": "npm run debug && mvt build --jsx-factory=h",
    "debug": "cross-env DEBUG=mvt:*",
    "serve": "serve dist"
  },
  "dependencies": {
    "preact": "^10.4.1",
    "vue-router": "^4.0.0",
    "vuex": "^4.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "cross-env": "^7.0.3",
    "postcss-nesting": "^7.0.1",
    "pug": "^2.0.4",
    "sass": "^1.26.3"
  }
}
